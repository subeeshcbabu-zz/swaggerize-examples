'use strict';
/**
 * Operations on /users/login
 */
module.exports = {
    /**
     * 
     * parameters: username, password
     * produces: application/json, application/xml
     */
    get: function loginUser(req, res) {
        res.sendStatus(501);
    }
};