'use strict';

/**
 * Operations on /pets/{id}
 */
module.exports = {
    
    /**
     * Returns pets based on ID
     * parameters: id
     * produces: application/json, text/html
     */
    get: function getPetsById(req, res) {
        res.sendStatus(501);
    }
    
};
